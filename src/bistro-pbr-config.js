// Set to true after scripts/download-real-bistro-pbr.mjs completes. The
// runtime keeps the locally derived channels as a deterministic fallback.
export const BISTRO_REAL_PBR = {
  enabled: false,
  wall: './assets/orca/bistro/real-pbr/wall',
  ground: './assets/orca/bistro/real-pbr/ground',
};
